/** Shared component contract types — identical between web and native. */

export type Size = 'sm' | 'md' | 'lg';

export type Variant =
  | 'primary'
  | 'secondary'
  | 'accent'
  | 'outline'
  | 'ghost'
  | 'link'
  | 'danger';

export type Status = 'info' | 'success' | 'warning' | 'danger';
