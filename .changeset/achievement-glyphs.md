---
'@muja-ui/icons': minor
---

Add seven glyphs for achievement and award UIs: `trophy`, `medal`, `crown`, `sparkles`, `thumbs-up`, `handshake` and `activity`.

These cover the icon vocabulary a backend-driven achievement list needs — a record that says "1st place" or "MVP" should not fall back to the same generic award mark as everything else.
